% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestrator.R
\name{radiomicsValidateMasksDS}
\alias{radiomicsValidateMasksDS}
\title{Validate that segmentation masks correspond to a set of images}
\usage{
radiomicsValidateMasksDS(generation_id_enc, dataset_id)
}
\arguments{
\item{generation_id_enc}{Encoded generation_id.}

\item{dataset_id}{Character; the imaging dataset being validated against.}
}
\value{
List with valid (logical), n_valid, n_missing, n_failed,
  and a mask_manifest (sample_id -> mask_path mapping) for valid masks.
}
\description{
DataSHIELD AGGREGATE method. Given a generation_id (from a segmentation
run), verifies that:
\enumerate{
  \item Each completed mask artifact still exists on disk (dsJobs artifact)
  \item Each mask's derivation_hash matches the source image's content_hash
  \item The generation covers the requested sample set
}
}
\details{
If masks are missing (expired dsJobs artifacts), returns which samples
need regeneration. Does NOT auto-regenerate.
}
